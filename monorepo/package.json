{
  "name": "antelope-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd frontend && npm run start",
    "build": "cd frontend && npm run build",
    "heroku-postbuild": "cd frontend && npm install && npm run build && cd ../api && pip install -r requirements.txt"
  },
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  }
} 